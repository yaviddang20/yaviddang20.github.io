/**
 * A render pass used to render post-effects.
 */
export class RenderPassPostprocessing extends RenderPass {
    constructor(device: any, renderer: any, renderAction: any);
    renderer: any;
    renderAction: any;
}
import { RenderPass } from '../../platform/graphics/render-pass.js';
